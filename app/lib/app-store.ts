// The App Store listing. Country-less /app/id<…> form so it resolves to
// the visitor's own storefront (not pinned to /ca/). Surfaced on the
// marketing layer so web visitors reach the one-tap App Store install
// instead of only the PWA add-to-home-screen path.
export const APP_STORE_URL = "https://apps.apple.com/app/id6774968763";
