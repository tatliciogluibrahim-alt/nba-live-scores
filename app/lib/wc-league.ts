// Single source of truth for the Summer Soccer (World Cup) ESPN league slug.
// Previously each WC route declared its own `const WC_LEAGUE = "fifa.world"`;
// this centralizes it so the value never drifts across surfaces.
export const WC_LEAGUE = "fifa.world";
